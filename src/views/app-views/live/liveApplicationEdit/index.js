import React from "react";
import LiveApplication from "../liveApplication";
const LiveEdit = () => {
  return (
    <LiveApplication
      mode="EDIT"
    />
  );
};

export default LiveEdit;
