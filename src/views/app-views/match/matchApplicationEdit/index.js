import React from "react";
import MatchApplication from "../matchApplication";
const MatchEdit = () => {
  return (
    <MatchApplication
      mode="EDIT"
    />
  );
};

export default MatchEdit;
